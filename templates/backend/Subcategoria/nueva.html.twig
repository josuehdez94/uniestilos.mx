{% extends 'backend/Categoria/indexCategoria.html.twig' %}
{% block title %}Nueva subcategoria{% endblock %}

{% block content %}
<div class="contenido mt-2">
    <div class="page-title-area">
        <div class="row align-items-center">
            <div class="col-sm-6">
                <div class="breadcrumbs-area clearfix">
                    <ul class="breadcrumbs pull-left">
                        <li><a href="{{ url('backend_categoria_index') }}">Lista de categorias</a></li>
                        <li><a href="{{ url('backend_categoria_detalles', {'id': categoria.id }) }}">{{ categoria.nombre }}</a></li>
                        <li><a href="{{ url('backend_subcategoria_categoria', {'categoria_urlAmigable': categoria.urlAmigable }) }}">Subcategorias</a></li>
                        <li><span>Nueva</span></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <hr>
    <h4>Nueva subcategoria de {{ categoria.nombre }}</h1>
    {{ include('backend/Subcategoria/_form.html.twig') }}
</div>
    {# <a href="{{ path('backend_subcategoria_categoria') }}">back to list</a> #}
{% endblock %}
